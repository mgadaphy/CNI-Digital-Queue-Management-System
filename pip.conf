[global]
# Prefer pre-built binary wheels over compiling from source.
# This prevents "Microsoft Visual C++ 14.0 or greater is required" errors
# on machines without Visual Studio Build Tools installed.
prefer-binary = true
